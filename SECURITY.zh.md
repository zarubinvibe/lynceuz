# 安全与隐私

<p align="center"><img src="docs/assets/pantheon/doc-security.png" alt="一扇上了金色门闩的白色大理石门紧闭着，三条蓝色水流停在门脚前，旁边立着封好印的证据箱" width="100%"></p>

Lynceuz 抓取公开网页，并把证据写到你的磁盘上。这一页写清楚它碰什么、拒绝什么，以及发现问题往哪里报。

## 发现漏洞往哪里报

请在本仓库开一条 [GitHub 安全公告](https://github.com/zarubinvibe/lynceuz/security/advisories/new)。可被利用的问题，请不要开公开 issue。写上你运行的命令、目标，以及你原本期待什么。

首次回复一般在七天之内。如果修复会改变别人依赖的行为，发布说明里会写明。

## 它能够到哪里

| 面 | Lynceuz 做什么 |
|---|---|
| 网络 | 向你给出的公开地址发出 HTTP(S) 请求，外加该主机的 `robots.txt` 和站点地图。 |
| 内网地址段 | 拒绝。回环地址、链路本地地址和 RFC 1918 地址根本走不到抓取那一步。 |
| 搜索引擎 | 完全不碰。Google、Bing 及其同类按设计就在范围之外。 |
| 文件 | 读它自己的仓库；只写进你选定的数据目录。 |
| 外部进程 | 启动 Node；只有渲染路径才会用到 Python 和浏览器。 |
| 花费 | 预算是零。没有哪个开关会悄悄打开付费通道。 |
| 遥测 | 没有。你的运行不会向任何地方上报。 |

## 机器人协议与限速

`robots.txt` 是整份读的，不只读 `Disallow` 那几行。禁止项从不绕过：既不直接绕，也不借浏览器作为子资源绕。站点地图和 `Clean-param` 被当作站点对爬虫的邀请，因为它们本来就是。并发数和页数都有上限。

## 浏览器路径

绝大多数工作根本不开浏览器。当一个页面只有像人一样打开才拿得到时，渲染路径会在一个专用的低权限账号下运行，而不是你的账号。在 macOS 上，一条绑定到该账号 UID 的包过滤规则，会挡掉除本地代理监听的那个回环端口之外的所有出站连接。

这条规则由你自己用一份可以读的脚本装上，并且能挺过重启：重启就消失的规则不叫隔离，Lynceuz 也拒绝把它叫作隔离。在信任浏览器之前，一只金丝雀会去检查代理端口和三条泄漏通道——直连 TCP、UDP 和 QUIC——并要求每一条给出真实回应，而不是一次成功的 `send()`。金丝雀跑不起来，浏览器就不打开，运行结果会说明原因。

## 密钥

Lynceuz 不需要 API key，不需要令牌，也不需要账号，更不会读你的。如果你给的地址把凭证直接写在查询串里，这个地址会进入清单，因为清单记录的就是真正抓取过的东西。所以不要把密钥放进地址。

## 怎么回滚

每次运行都会写下内容文件和一份 JSON 清单：引擎、尝试过的路径、时间、SHA-256 和花费。删掉数据目录，这次运行就什么都不剩。没有守护进程，没有开机启动项，没有后台服务。

唯一会改动系统的是 macOS 的隔离安装脚本，它自带 `--rollback`，会把它加过的账号、锚点和规则都撤掉。

## 诚实的边界

- 隔离目前只在 macOS 上得到证明。在别的系统上，浏览器路径保持关闭，而不是假装能用。
- 拦你的站点会继续拦你。Lynceuz 会报告这扇门是关着的，而不是编造门后有什么。
- 这里的一切都不破解验证码。检测到挑战后，运行会放慢或停下，接下来由人来决定。
